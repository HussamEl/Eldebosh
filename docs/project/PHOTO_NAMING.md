# تسمية الصور — v1.3

> **من صورة إلى ثلاث لكل منتج.** الموقع يبدّلها تلقائياً بلا سكربت.
>
> **`v1.3` في `2026-09-20`: صُحِّحت الصيغة.** كانت الوثيقة تقول `.jpg` بينما كل
> صورة في المستودع `.webp`، وأكثرها يحمل `ASIN` وبعضها يحمل `-v2`. فمن اتّبع
> الوثيقة أنتج اسماً لا وجود له. **الوثيقة تصف الواقع، أو تُحذف.**

---

## 1. الصيغة

```
P-NN-K.webp                 الشكل الأساسي
P-NN-K-<ASIN>.webp          حين يُعرف الـASIN وقت الرفع
P-NN-K-v2.webp              حين تُستبدل صورة بأخرى
```

| الجزء | المعنى |
|---|---|
| `P` | منتج |
| `NN` | رقم المنتج — خانتان، من حقل `code` في ملف المنتج |
| `K` | رقم الصورة: `1` أو `2` أو `3` |

**أمثلة حقيقية من المستودع:**

```
P-02-3.webp                 بسيطة
P-22-1-B08D6NCQ1Z.webp      ومعها ASIN
P-01-3-v2.webp              نسخة بديلة
P-20-1-B0GV79Z1W6-v2.webp   الاثنان معاً
```

**الصيغة `webp` حصراً، مربعة ٧٢٠ بكسل.** ولا `jpg` ولا `png` في `public/uploads/`.

**ورقم المنتج هو حقل `code` في ملفه** — `P-24` مثلاً. يُسنَد مرة ولا يتغيّر،
ويطبع `npm run state` أعلى رقم مستعمل. **والفحص يرفض رمزاً مكرراً** منذ
`2026-09-20`، لأن منتجين بالرمز نفسه يتقاسمان صورهما.

**والفحص يرفض أيضاً صورةً مذكورة في ملف منتج بلا ملف لها على القرص.**

---

## 2. ماذا تصوّر في كل صورة

| الصورة | ماذا تُظهر |
|---|---|
| `1` | **المنتج وحده** — خلفية بيضاء، من الأعلى مباشرة |
| `2` | **المقياس** — بجانب بطاقة بنكية أو هاتف |
| `3` | **قيد الاستخدام** — في السيارة، على المكتب، في الحقيبة |

**الصورة الأولى إلزامية.** الثانية والثالثة تزيدان الثقة ولا تُشترطان.

**الثالثة هي الأقوى تجارياً** — لا يستطيع أي منافس تزييفها.

---

## 3. كيف يبدّلها الموقع

| عدد الصور | السلوك |
|---|---|
| واحدة | ثابتة |
| اثنتان | تبديل متقاطع كل ٤٫٥ ثانية |
| ثلاث | تبديل متقاطع كل ٤٫٥ ثانية بالدور |

**بلا JavaScript إطلاقاً** — تبديل بالـCSS وحده. لا يبطّئ الصفحة ولا يفتح جزيرة تفاعلية جديدة.

**التمرير بالمؤشر يوقف التبديل** ليتأمل الزائر الصورة.

**ومن يفضّل تقليل الحركة في إعدادات جهازه يرى الصورة الأولى فقط** — احترام لإعدادات الوصولية.

**وشارة صغيرة تعرض عدد الصور** أعلى البطاقة حين تكون أكثر من واحدة.

---

## 4. المواصفات التقنية — مقيسة من المرفوع فعلاً

> **صُحّحت في `2026-09-04`.** كان مكتوباً هنا «١٦٠٠ بكسل» و«خلفية مزالة»،
> ولا الصور تقول ذلك ولا السكربت الذي يخبزها. الوثيقة تتبع الممارسة لا العكس.

| البند | القيمة |
|---|---|
| المقاس | **`720×720` مربّع** |
| الصيغة | `webp` |
| الحجم | أقل من ٣٠٠ كيلوبايت — تنزل الجودة تدريجياً حتى يقع تحته |
| الشكل | **المنتج كاملاً**، والفراغ حوله ضبابية من الصورة ذاتها ممزوجة بلون الهوية، والحواف متلاشية فلا يظهر إطار |
| النص البديل | إلزامي بالسويدية |

**والشكل ليس ذوقاً بل بنية:** هو ما يجعل عشرين بطاقة من عشرين خلفية مختلفة
تبدو مجموعة واحدة. ولذلك لا تُعالَج صورة باليد ولا بأداة أخرى:

```
python3 scripts/make_tiles.py <الصورة الخام> P-21-1 [ASIN]
```

يخبز الشكل، ويسمّي، ويكتب `P-NN-1-<ASIN>.webp` حين يُمرَّر رقم أمازون —
**اسماً واحداً لا نسختين.** ويرفض اسماً غير صالح، أو `ASIN` على غير الصورة
الأولى.

> **درسٌ من اليوم نفسه:** كتبتُ سكربتاً ثانياً للصور دون أن أقرأ هذا،
> فأنتج أربع بطاقات مقصوصة قصّاً حادّاً لا تشبه العشرين الأخرى. العطب لم يكن
> في الوصفة الثانية، **بل في وجود وصفتين**. حُذف الثاني، وأُعيد خبز الأربع.

**احتفظ بالأصول** في مجلد خارج المشروع. المرفوع نسخة مخبوزة.

---

## 5. دورة العمل

**١.** تصوّر الصور.

**٢.** تسمّيها بالصيغة أعلاه.

**٣.** ترسلها **كملفات** — لا كصور داخل المحادثة.

> **الفرق ليس شكلياً.** الصورة الملصقة في المحادثة أراها ولا أستطيع فتحها ولا
> إعادة ترميزها — لا وجود لها على القرص عندي. والطرق التي تضع الملف فعلاً:
> رفعه من `/admin/`، أو سحبه إلى `public/uploads/` من واجهة غيتهب.

**٤.** أعالجها بالسكربت أعلاه وأربطها بالمنتج وأدفعها.

**٥.** يُعاد توليد الفهرس:

```
npm run assets
```

---

## 6. أنواع أخرى

| البادئة | النوع | المكان |
|---|---|---|
| `P` | منتج | `public/uploads/` |
| `B` | هوية — شعار وأيقونة | `public/` |
| `S` | لقطة شاشة مرجعية | `assets/screens/` |

**لقطات الشاشة في مجلد منفصل** لأنها مرجعية للعمل ولا تُنشر على الموقع.

---

## 7. قاعدة ملزمة

**ممنوع عرض صورة منتج تحت اسم منتج آخر** — ولو مؤقتاً وللاختبار.

الصورة ادعاء بصري، وعرض صورة خاطئة اختلاق مثل اختراع مواصفة.

**إن لم تتوفر صورة، تُترك البطاقة بلا صورة** فتعرض الرقم الأهم في المواصفات بدلاً منها.
