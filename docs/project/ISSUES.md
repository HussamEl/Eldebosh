# سجل المشاكل

> كل مشكلة قائمة مع **دليلها** و**ما جُرِّب** و**الخطوة التالية**.
>
> **الغرض:** ألا نكرر محاولة فاشلة، وأن يفهم من يستلم المشروع المشكلة في دقيقتين.
>
> **صيغة الإضافة:** الرقم · العنوان · الخطورة · العَرَض · الدليل · ما جُرِّب · التشخيص · الخطوة التالية · الحالة.

---

## I-001 · ووردبريس يتحكم بالدومين

**الخطورة:** 🔴 حاجب

**العَرَض:** الموقع الثابت لا يعمل. المسارات تُحوَّل إلى ووردبريس.

**الدليل:** بعد التثبيت ظهرت في المجلد العام:

```
index.php
wp-admin/  wp-includes/  wp-content/
wp-config.php
.htaccess
```

**التشخيص:** `index.php` صار نقطة الدخول بدل `index.html`، و`.htaccess` يحوّل **كل** طلب إلى ووردبريس حتى الصفحات التي لا يملكها.

**ما جُرِّب:** رفع الموقع الثابت فوقه — لم ينفع، لأن `.htaccess` يعترض قبل وصول الطلب إلى الملفات.

**ما حُلّ به:** حُذف المجلد العام كاملاً، **بما فيه الملفات المخفية** — بقاء
`.htaccess` وحده كان يُبقي التعطيل. ثم رُفع الموقع الثابت على مجلد نظيف.

**الحارس بعده:** ووردبريس ممنوع على هذا الدومين — `CLAUDE.md` و`D-014`. وإن
لزم مستقبلاً فعلى نطاق فرعي منفصل، لا على `/public_html/`.

**الحالة:** مغلقة — `eldebosh.com` يخدم الموقع الثابت.

---

## I-002 · النشر التلقائي معطّل

**الخطورة:** 🔴 حاجب

**العَرَض:** GitHub Actions يبني بنجاح ثم يفشل عند الرفع.

**الدليل:**

```
Error: Timeout (control socket)
```

المحاولتان — المشفّرة والعادية — تنتهيان بـ`2m 0s` **بالضبط**، وهي المهلة كاملة.

**التشخيص:** الاتصال لم يُفتح أصلاً. لم يصل إلى مرحلة كلمة المرور. نجح مرة واحدة ثم توقف نهائياً — **نمط حجب تلقائي للعنوان**.

**ما جُرِّب:**

| المحاولة | النتيجة |
|---|---|
| إعادة تشغيل العملية | فشل |
| رفع المهلة إلى دقيقتين | فشل |
| `FTPS` مشفّر | فشل |
| `FTP` عادي كبديل تلقائي | فشل |
| تحديث أداة النشر | فشل |
| إعادة ضبط الأسرار الأربعة | فشل |

**الخطوة التالية:**

**١.** شغّل أداة التشخيص:

```
Actions ← Diagnose FTP connection ← Run workflow
```

**ما حُلّ به:** عُكس اتجاه النشر. لم تعد غيتهب تدفع بالـ`FTP` — صارت تبني
وتفحص وتحدّث فرع `deploy`، ثم يوقظ `Webhook` الاستضافةَ فتسحب منه. السبب أن
خوادم غيتهب محجوبة عن الدخول، والاستضافة وحدها تستطيع الخروج.

**التفصيل:** `docs/12-deploy-from-github.md`.

**الأثر الجانبي:** لوحة التحرير تكتب في GitHub، وبلا نشر تلقائي **لا يصل ما يُكتب إلى الموقع**.

**الحالة:** مغلقة — قيس في `2026-09-04`: ختم البناء الحيّ ساوى وقت البناء.

---

## I-003 · ملفات قديمة تبقى على الخادم

**الخطورة:** 🟠 متكرر

**العَرَض:** بعد رفع نسخة جديدة، بعض الصفحات تعرض التصميم القديم.

**الدليل:** صفحة فئة تعرض سبع فئات فرعية بتصميم أخضر، بينما الرئيسية زرقاء وتعرض واحدة.

**التشخيص:** الرفع **يستبدل الموجود ولا يحذف المفقود**. الصفحات التي لم تعد تُولَّد تبقى وتعمل، ويصل إليها الزائر من قوائم قديمة.

**ما جُرِّب:** الرفع فوق القديم — فشل **ثلاث مرات**.

**الحل المطبَّق:**

- بصمة بناء في كل صفحة تكشف النسخة المعروضة
- `SERVER_FILES.md` يعدّد المجلدات الصحيحة
- خيار حذف تلقائي في النشر — **يعمل حالما يعود الاتصال**

**الخطوة التالية:** احذف كل شيء قبل كل رفع يدوي — `docs/UPLOAD.md`.

**الحالة:** محلولة بإجراء، تتكرر إن أُهمل الإجراء.

---

## I-004 · منتجات بلا روابط أمازون

**الخطورة:** 🟠

**العَرَض:** منتجات مصوَّرة وجاهزة تظهر بعلامة `Länk kommer` بلا زر شراء.

**الدليل:** انظر `STATE.md` — قسم "تنتظر ASIN".

**التشخيص:** ينقصها `asin` و`source_url`، وبعضها ينقصه الاسم الحقيقي والطراز.

**ولا عدد مكتوب هنا عمداً** — الرقم يتقادم في ساعات. يُقرأ من `npm run state`،
سطر «تنتظر ASIN».

**الخطوة التالية:** صاحب المشروع يفتح صفحة كل منتج على أمازون ويرسل الرابط.

**الأثر:** كل رابط ناقص = منتج لا يُشترى = عمولة ضائعة.

**الحالة:** مفتوحة.

---

## I-005 · قسم الساحة معطّل

**الخطورة:** 🟠

**العَرَض:** صفحة التواجد تعمل، لكن لوحة الاتصال لا تظهر.

**التشخيص:** الفحص الآلي يرفض النشر برقم هاتف نائب — **وهذا مقصود** حتى لا يُنشر رقم وهمي.

**ما حُلّ به:** وُضع الرقم بصيغة دولية في `src/data/torget/torget.yaml` وصار
`active: true`.

**والحارس باقٍ:** الفحص الآلي ما زال يرفض النشر برقم هاتف نائب. لا تُعطّله.

**الحالة:** مغلقة.

---

## I-006 · النصوص القانونية مسوّدات

**الخطورة:** 🟠

**العَرَض:** أربع صفحات تحمل علامة `UTKAST`.

**التشخيص:** سياسة الخصوصية تنتظر اختيار أداة التحليلات، وسياسة الكوكيز تتبعها.

**ما حُلّ به:** كُتبت النصوص الستّ ونُشرت بلا `UTKAST`، وكلها اليوم
`stage: published`. وسياسة الكوكيز تصف الواقع: لا كوكيز ولا تتبّع، فلا نافذة
موافقة.

**وما بقي منفصلاً عنها:** اختيار أداة التحليلات — مهمة `T5` في `TASKS.md`،
والتوصية بلا كوكيز حتى تبقى هذه النصوص صحيحة كما هي.

**الحالة:** مغلقة.

---

## I-007 · تصنيف دخل الأفلييت ضريبياً

**الخطورة:** 🟠 قانوني

**العَرَض:** غير معروف هل يُعامل دخل الأفلييت كنشاط الساحة أم يُصنَّف منفصلاً.

**التشخيص:** بيع الساحة نشاط محدود يُصرَّح عنه. لكن دخل الأفلييت **منتظم ومستمر وبنية ربح**، وهذه صفات قد تنقله إلى تصنيف مختلف.

**الخطوة التالية:** استشارة مصلحة الضرائب — مجانية. الأسئلة في `PROJECT_CONTEXT`.

⚠️ **لا يُبنى على تخمين.**

**الحالة:** مفتوحة.

---

## I-009 · نصّ التجربة قالبي في ثلاثة منتجات

**الخطورة:** 🔴 ثقة

**العَرَض:** حقل `hands_on` يحمل الجملتين نفسيهما — لا يتغيّر فيهما إلا الشهر —
في `P-01` و`P-04` و`P-11`. وصفحة `basta-powerbank-2026` المنشورة تعرض الجملة
**مرتين على الصفحة الواحدة**، لمنتجين مختلفين.

**التشخيص:** التطابق الحرفي في موضع التجربة يقول للقارئ إن أحداً لم يكتبها عن
تجربة. وهذا نفسه ما نأخذه على المنافسين في `CONTEXT §3`، فهو يهدم الفارق الذي
يقوم عليه الموقع كله. حُذف النصّ نفسه من `P-02` و`P-03` لأنهما `tested: false`
ولا يجوز أن يحملا جملة أداء — ولم يكن ظاهراً عندهما أصلاً، لأن `HandsOn.astro`
لا يعرض `hands_on` إلا عند `tested: true`.

**الخطوة التالية:** جملة حقيقية واحدة عن كل منتج، يلاحظها **حسام** بنفسه ولا
تُنسخ من صفحة بائع — زرٌّ يصعب ضغطه بالقفاز، مغناطيس يفلت مع جراب معيّن، سخونة
في الجيب. يكتبها بالعربية، ويصوغها `Claude Project` بالسويدية.

**الحارس:** `scripts/validate.mjs` قاعدة `5c` تُنبّه على كل نصّ تجربة يتكرر بين
منتجين، بعد تقنيع الشهور والأرقام. تُرفع إلى خطأ يوم تصل الجمل الحقيقية.

**الحالة:** مفتوحة.

---

## I-010 · لا وزن في مواصفات المنتجات

**الخطورة:** 🟡 محتوى

**العَرَض:** `key_specs` لا تحوي حقل وزن في أي منتج. وأقسام الوزن في
`10000-vs-20000-mah` و`basta-powerbank-2026` مكتوبة بلا رقم واحد لهذا السبب.

**التشخيص:** الوزن مواصفة موثّقة عند المصنّع لا قياساً منّا، فهو مسموح بالكامل.
لكنه غير موجود في البيانات، ولا يُكتب من الذاكرة.

**من يجلبه:** ليس أنا — `www.amazon.se` محجوب من بيئة التنفيذ (`403` من الوسيط)،
فلا أقرأ صفحة المنتج. يجلبه **حسام** من صفحة كل منتج، أو **جيميناي** من صفحة
المصنّع. أربعة منتجات فقط: `P-01` و`P-02` و`P-03` و`P-04`.

**بعده:** أضيف `vikt` إلى `key_specs` مع `source_url` و`last_verified`، ويضيف
`Claude Project` الأرقام إلى القسمين.

**حدّ تماس مؤجَّل:** موضوع الوزن انتقل إلى `10000-vs-20000-mah`، فقسم
`Vikt är den dolda kostnaden` في `basta-powerbank-2026` صار مكرَّراً. لا يُلمس
حتى يرسل `Claude Project` تقليصه.

**الحالة:** مفتوحة.

---

## I-011 · جواب جيميناي الأول لا يصلح للنشر كما هو

**الخطورة:** 🔴 ثقة

**العَرَض:** أول رد بحثي (`EB-008`) يحمل أربعة اقتباسات بروابط، وثلاثة أجوبة
«لا مصدر». ولا شيء منه قابل للاستعمال قبل تحقّق بشري.

**التشخيص — ثلاثة عيوب، أولها يُبطل الباقي:**

**١. تناقض داخلي.** البند ٣ يعلّل «لا مصدر» بـ«عدم توفر الوصول للبحث المباشر».
فإن لم يستطع التصفّح، فمن أين جاءت اقتباسات البنود ١ و٢ و٤ و٥؟ الاحتمال الراجح
أنها من الذاكرة — وهو ما تمنعه المادة ٦٫١ نصّاً، وهو ما بُني له نصّ التكليف.

**٢. رابط مغسول.** مصدر `SAS` في البند ٤ ليس صفحة `SAS`، بل رابط بحث في غوغل
يحوي العنوان: `google.com/search?q=https://www.flysas.com/…`. هذا استشهاد
بصيغة مصدر، لا مصدر.

**٣. دمج بلا سند.** البند ٥ يطلب `Apple` و`Samsung` و`Anker` برابط لكل واحد،
والجواب يعطي رابط `Apple` وحده ثم ينسب المدى إلى `Apple` و`Samsung` معاً.
و`Anker` غائبة كلياً.

**ما لا أستطيعه:** التحقق من الروابط نفسها. الثلاثة محجوبة عن بيئة التنفيذ
(`000` من الوسيط)، فلا أؤكّد ولا أنفي مضمونها.

**القاعدة التي خرجت منها — تسري على كل طلب بحثي، لا على ذلك الطلب وحده:**

1. **يصرّح الباحث في أول سطر هل تصفّح فعلاً أم لا.** جوابٌ لا يقول ذلك يُردّ.
2. **كل رابط رابطٌ مباشر إلى الصفحة نفسها.** رابط بحث في غوغل يحمل العنوان
   ليس مصدراً، وهو أخطر من غياب المصدر لأنه يشبهه.
3. **«لا مصدر» جوابٌ مقبول ونافع.** ما لا يُتصفَّح يُكتب «لا مصدر» بلا اقتباس،
   وتُخفَّف الجملة في الصفحة إلى نصيحة بلا تعليل — كما جرى في
   `mobilen-dor-i-kylan`.
4. **لا يُنشر شيء من ردّ بحثي قبل تحقّق بشري من الروابط.**

**وما لا يستطيعه `Claude Code`:** فتح تلك الروابط بنفسه — نطاقاتها محجوبة عن
بيئة التنفيذ. فالتحقق عمل حسام، لا عمله.

**الحالة:** قاعدة قائمة — تُقرأ قبل أي تكليف لجيميناي.

---

## I-012 · البحث والكتابة صارا عند طرف واحد

**الخطورة:** 🟠 منهج

**العَرَض:** بأمر حسام في `2026-09-03`، تولّى `Claude Project` جلب المصادر
بنفسه، ورفض حسام أن يتحقق من عيّنة. فصار من يجلب المصدر هو من يكتب النصّ
الذي يستشهد به.

**التشخيص:** السلسلة كانت ثلاثية — جيميناي يجلب، `Claude Project` يكتب،
`Claude Code` يفحص. وقد سقط طرفان: الجالب المستقل، والمدقّق البشري. وأنا لا
أسدّ الفراغ: `iata.org` و`apple.com` و`flysas.com` وغيرها محجوبة عن بيئة
التنفيذ (`000` من الوسيط)، فلا أفتح صفحة ولا أقارن اقتباساً بمصدره.

**عُرض الثمن على حسام مرتين وأكّد قراره.** فهو قرار مالك لا سهو، ويُنفَّذ.

**ما بقي من الفحص المستقل — وهو الشكل وحده:**

- القاعدة `7b`: كل رابط مصدر يجب أن يكون مباشراً. تُرفض محرّكات البحث
  والوسطاء وذاكرة غوغل وscreens الترجمة، ويُرفض أي رابط يلفّ رابطاً في
  معاملاته. أمسكت عطب `SAS` الحقيقي عند زرعه.
- القاعدة `7`: `publisher` و`url` و`accessed` إلزامية.
- مخطط `externalSource`: `type` و`claim` إلزاميان كذلك.

**ما لا يمسكه أي فحص:** أن يكون الاقتباس فعلاً على الصفحة. **هذا الخطر باقٍ
ومعلَن.**

**التخفيف القائم:** كل رقم منشور يحمل رابطه وتاريخه في `sources`، فالقارئ
يفتح الرابط ويحكم. الرقابة انتقلت من داخل الفريق إلى الجمهور.

**الحالة:** مفتوحة — بقرار المالك، لا بانتظار حلّ.

---

## I-013 · لا يُستشهد بصفحة إقليمية

**الخطورة:** 🟠 منهج

**العَرَض:** صفحات الشركات الكبرى تُنشر بنسخ لغوية وإقليمية متعددة، وقد
تتناقض.

**مثال مقيس:** صفحة `SAS` الإيطالية تقول «حتى ٢٠ بطارية **أو** بطارية
محمولة»، والإنجليزية العامة تقول «٢٠ بطارية **وبحد أقصى ٢** محمولة». شركة
واحدة، وصفحتان تختلفان في العدد.

**القاعدة:** يُستشهد بالنسخة العامة أو نسخة السوق الذي نخاطبه، وتُطابَق مع
مصدر ثانٍ مستقل قبل النشر. **وإن تعارضتا، تُقدَّم النسخة الموافقة للسلطة
الوطنية.**

**لماذا تُسجَّل ولا تُفحَص آلياً:** لا يفرّق فحص بين نسخة عامة وإقليمية من
النطاق وحده — `flysas.com/en/…` و`flysas.com/it/…` كلاهما رابط مباشر سليم.
والقاعدة `7b` تمسك شكل الرابط لا مضمونه. **فهذه قاعدة على الكاتب لا على
البرنامج**، ولذلك مكانها هنا لا في `validate.mjs`.

**الحالة:** قاعدة قائمة.

---

## I-014 · `magsafe-vs-qi2-vs-tradlos` لا تُنشر كمقارنة

**الخطورة:** 🟡 بنية

**العَرَض:** أمر حسام بنشر الخمس، ونُشرت أربع. الخامسة أوقفها الفحص:

```
مقارنة منشورة بعدد منتجات 0 (المطلوب 2–4)
```

**التشخيص:** ليس خطأً في الصفحة ولا في القاعدة — **بل في تصنيفها.** الصفحة
تقارن **معايير شحن** (`MagSafe` · `Qi2` · اللاسلكي البسيط) لا منتجات، و
`products: []` فيها قرار مقصود من `Claude Project`. ومجموعة `comparisons` في
المشروع تعني مقارنة منتجات، ولهذا تفرض ٢–٤.

**الخياران — والقرار تحريري لا برمجي:**

| # | الحل | الثمن |
|---|---|---|
| ١ | تُنقل إلى `posts` كشرح | يتغيّر مسارها من `/compare/` إلى `/blog/` — والـ`slug` لم يُنشر بعد فلا إعادة توجيه |
| ٢ | تُضاف إليها ٢–٤ منتجات | يحوّل شرح معيار إلى صفحة تبيع، وهو ما رفضه `Claude Project` عمداً في صفحة الطيران للسبب نفسه |

**رأيي:** الأول. الصفحة شرحٌ لا مقارنة منتجات، والتصنيف هو الخاطئ لا المحتوى.
**ولا أنفّذه** — بنية المحتوى قرار تحريري.

**القرار `2026-09-03`:** `Claude Project` اختار الأول — تُنقل إلى `posts`.
وحجّته على رفض الثاني أقوى من حجّتي: **إضافة منتجات لإرضاء فحص تعني أن الفحص
يقود المحتوى**، ولو فُعلت هنا لفُعلت في صفحة الطيران غداً. نُقلت، وحُذف حقل
`verdict` لأنه حقل المقارنات وحدها، ونُشرت.

**الحالة:** مغلقة.

---

## I-015 · البوابة كانت حمراء في Actions ولم أنظر إليها

**الخطورة:** 🔴 منهج

**العَرَض:** حسام فتح تبويب `Actions` ووجد ما لم أجده: `verify` فاشل منذ
الدفعة `#44`، و`deploy` فاشل في `#35` و`#36`. وكنتُ أُبلّغ «اجتاز» في كل مرة.

**ثلاثة أسباب، كلها مني:**

**١ · نمط مضمّن في `SourceList.astro`.** الفحص `audit` يمنع أنماط التخطيط
المضمّنة. لم يظهر العطب إلا حين نُشرت أول صفحة تحمل `sources`. صار صنفاً
`.sources-note`.

**٢ · انحراف `site/` — وأنا صنعته.** كنتُ أشغّل `git checkout -- site/` بعد كل
فحص «لتنظيف» الكوميت، فأرمي ناتج البناء الذي يقارنه `check:drift` بعد ذلك.
**الحارس كان مصيباً في كل مرة**، وأنا أطمس ما يقيسه. ناتج البناء يُدفَع من
الآن: بصمة البناء وترتيب مفاتيح `Pagefind` يُقنَّعان في الحارس أصلاً لهذا
الغرض بالذات.

**٣ · قراءة رمز الخروج من أنبوب.** كنتُ أكتب:

```
npm run verify 2>&1 | grep … ; echo "EXIT=$?"
```

و`$?` هنا رمز خروج `grep` لا `npm`. **فكل «EXIT=0» أعلنتُه كان يقيس الأمر
الخطأ.** وقد كان `audit` يسقط محلياً في الجولات نفسها التي سمّيتها خضراء.

**والالتفاف البنيوي:** تشغيل `publish-deploy-branch.mjs` بيدي كان يوصل المحتوى
إلى فرع `deploy` **رغم أن البوابة حمراء** — أي أن خطوتي اليدوية تلتفّ حول
الفحص المبني لمنع ذلك. صار السكربت يشغّل البوابة قبل النشر ويرفض عند أي سقوط،
ويتخطّاها داخل `Actions` وحدها لأن سير العمل هناك شغّلها قبله.

**ما لم يتضرّر:** المحتوى وصل إلى `deploy` فعلاً في كل مرة، لأن السكربت اليدوي
نجح. فُحصت الصفحات الست على الفرع وكلها موجودة. **الموقع لم يفقد شيئاً — البوابة
هي التي فقدت معناها.**

**القاعدة الباقية:** رمز الخروج يُقرأ من الأمر نفسه لا من أنبوب بعده، والفشل
يُعلَن بخطوته باسمها.

**سبب رابع ظهر بعد إصلاح الثلاثة:** بقي `verify` أحمر في الدفعة `#48` على
**انحراف مختلف**، ويمرّ محلياً — أي فرق بيئة حقيقي لا تنظيفاً مني.

`docs()` كانت ترتّب بالتاريخ وحده:

```
.sort((a, b) => +b.data.updated - +a.data.updated)
```

وستّ صفحات تحمل `updated: 2026-09-03`. **وعند التعادل يعود الترتيب إلى ترتيب
قراءة الملفات من القرص**، وهو يختلف بين جهازي وخادم `GitHub`. فتختلف الصفحة
الرئيسية بين بنائين، ويختلف معها فهرس `Pagefind` وأسماء ملفاته المبنية على
تجزئة المحتوى — وهي الملفات التسعة بعينها في التقرير.

**الإصلاح:** فاصل عند التعادل في المواضع الثلاثة كلها — `slug` للمستندات
و`id` للفئات والمنتجات. وثبت أن بناءين متتاليين متطابقان بايتاً ببايت.

**والدرس أوسع من العطب:** أي ترتيب بمفتاح قابل للتعادل هو بناء غير قابل
للتكرار. حارس الانحراف وُضع لهذا بالذات، **وقد أدّى وظيفته — وأنا من لم أقرأ
تقريره.**

**وسبب خامس، وهو الأخير:** بعد إصلاح الترتيب نزل الانحراف من تسعة ملفات إلى
خمسة، **ولم يبقَ فيها ملف HTML واحد** — أي أن مخرجات الموقع صارت متطابقة. الخمسة
كلها فهرس `Pagefind` المُجمَّع: أسماؤه تُشتقّ من تجزئة المحتوى بواسطة **ثنائية
خاصة بالمنصّة**، فتختلف بين حاوية التنفيذ وخادم `GitHub`.

**وهذه ليست مخرجاتنا بل مخرجات الأداة.** مقارنتها بايتاً ببايت عبر منصّتين
مقارنةٌ للأداة لا للموقع. فاستُثني الفهرس المُجمَّع من الحارس، **وكل ما عداه
يُقارَن كما كان — وكل صفحة HTML في مقدّمته، وهي سبب وجود الحارس.**

**وثمن الاستثناء مدفوع:** أُضيف فحص وجود — لا بدّ من `.pf_index` و`.pf_meta`
فعلاً، وإلا فالبحث معطوب ويسقط البناء. أُثبت بحذف المجلد ومشاهدة الفحص يسقط.

**الحالة:** مغلقة — الأسباب الخمسة مُصلَحة، والبوابة خضراء بأحد عشر فحصاً
مقروءة برموز خروج حقيقية.

---

## I-016 · الهيكل الكامل — قرار حسام بتغيير قاعدة ٦٫٤

**الخطورة:** 🟠 قرار مالك · يخالف قاعدة مُلزمة

**القرار:** بتاريخ `2026-09-03` قرّر حسام أن يبقى هيكل الموقع كاملاً ظاهراً،
وأن يُقال للزائر صراحةً حين يصل إلى قسم لم يُملأ، مع دعوته إلى إرسال تجربته أو
سؤاله.

**وهذا يخالف المادة ٦٫٤ نصّاً:** «لا رابط يقود إلى صفحة فارغة».

**كيف نُفِّذ بحيث يبقى المعنى محفوظاً:**

| القيد | التنفيذ |
|---|---|
| لا تدخل نتائج البحث | `noindex, follow` على كل هيكل · وخارج `sitemap` تماماً |
| لا تبيع شيئاً | لا بطاقة منتج · لا جدول مقارنة · **ولا إفصاح تجاري** — إفصاحٌ على صفحة بلا روابط يربك القارئ ويضعف الإفصاح نفسه |
| لا تدّعي محتوى | نصّها يقول: «لم تُكتب بعد. وضعنا العنوان ولم نضع المحتوى — أفضل من نصّ لا نستطيع الوقوف خلفه» |
| تُعلَن قبل النقر | شارة `Inte skriven än` على البطاقة في كل قائمة |
| تعطي القارئ دوراً | بريد ورسالة — **بلا نموذج ولا تخزين**، فلا يتغيّر شيء في سياسة الخصوصية |

**العدد:** ٢٣ هيكلاً نُشرت · ٤٣ صفحة مفهرسة في `sitemap` · ٢٧ صفحة `noindex`.

**تحفّظي — سحبتُه، وكان خطأً مني:** كتبتُ هنا أنّ «أمازون تراجع الموقع قبل
قبول الحساب» وأنّ ٢٣ صفحة بلا محتوى «قد تُضعف الطلب»، واقترحتُ قلب الهياكل
إلى `published: false` قبل التقديم. **لا شيء من هذا ينطبق:** حسام مقبول في
البرنامج أصلاً، ومعرّف الشراكة `electro066-21` على `amazon.se` مثبَّت في
`CONTEXT.md` منذ أول يوم. فلا طلبَ قائماً كي يَضعُف، ولا قرارَ مطلوب من حسام
هنا، ولا تُقلب الهياكل قبل شيء. الذي بقي فعلاً هو عتبة **ثلاث مبيعات
مؤهَّلة** لفتح الـ API والصور والأسعار (المادة ٦٫٣) — وهي عتبة مبيعات، لا
مراجعة تصميم، ولا تقيس عدد الصفحات المكتوبة.

**والخطأ نفسه يستحق التسمية:** بنيتُ تحفّظاً على قاعدة لا أملك مصدرها،
واعترفتُ بذلك في السطر نفسه ثمّ كرّرتُها على حسام مرّة أخرى. الاعتراف بغياب
المصدر لا يُبيح البناء عليه — القاعدة ٧ تقول: ما لا مصدر له لا يُقال.

**قواعد الفحص المعدَّلة:** `5b` تقبل `published: true` مع `stage: draft`
كحالة ثالثة مشروعة · شرط ترشيحَي الدليل وشرط منتجات المقارنة يسريان على النصّ
التام وحده · `check:drafts` يتخطّى الهياكل ويبقى على الصفحات التي تدّعي
التمام ونصّها مسوّدة.

**ما يلزم تحريرياً:** صياغة المادة ٦٫٤ في `INSTRUCTIONS` لم تعد تصف الواقع.
النصّ لـ`Claude Project`. ونصوص `notwritten.*` في `ui.ts` كتبتُها أنا وهي
تحريرية — **راجعها.**

**الصياغة وصلت `2026-09-03`:** المادة ٦٫٤ في `INSTRUCTIONS v1.6` بنصّ
`Claude Project`، وفيها بندٌ لم يطلبه حسام وأضافه هو: **«الهيكل وعدٌ عليه
تاريخ — وما وقف تسعين يوماً بلا كتابة يُكتب أو يُسحب»**. وهي الجملة الوحيدة
القابلة للفرض في النصّ، فبُنيت: القاعدة `5g`.

**وضبط العدّاد:** الهياكل الثلاثة والعشرون كانت تحمل `updated: 2026-01-01` —
تاريخ إنشاء الهيكل لا نشره. فكانت تعرض على الزائر «Uppdaterad 2026-01-01» على
صفحة ظهرت اليوم، **ويبدأ عدّاد التسعين يوماً منتهياً قبل أن يبدأ**. صار
تاريخها يوم نشرها.

ونصوص `notwritten` روجعت: خطأ نحوي في السويدية، والسطر الرئيسي صار يجيب سؤال
القارئ الأول — لماذا الصفحة موجودة إن لم تُكتب.

**الحالة:** مغلقة.

---

## I-017 · سيران للعمل على الدفعة نفسها — والنشر لا يعتمد على البوابة

**الخطورة:** 🔴 بنية

**العَرَض:** حسام سأل لماذا تظهر عمليتان في `Actions` عند كل دفعة. الجواب أن
`verify.yml` و`deploy.yml` كانا ملفّين مستقلّين، كلاهما `on: push`.

**والتكرار ليس أسوأ ما فيه:**

**١ · بناء مزدوج.** كل دفعة تُنفّذ `npm ci` وبناءً كاملاً مرتين.

**٢ · كوميتان على `deploy` لكل دفعة.** قيس من تاريخ الفرع: كل رسالة نشر تتكرر
مرتين — واحدة من سكربتي اليدوي، وواحدة من سير العمل. سبع دفعات، أربع عشرة
عملية نشر.

**٣ · والخطير:** `deploy` كان يشغّل مجموعة فحوص **أقلّ** من `verify`، ولا
يعتمد عليه. فينجح النشر بينما البوابة حمراء. **وقع فعلاً:** الدفعة `deploy #34`
نجحت و`verify #45` سقطت على العَقِد نفسها، ومثلها `#33` مع `#44`. أي أن
الموقع نُشر مرتين على الأقل والبوابة ساقطة.

**الإصلاح:** سير عمل واحد بمهمّتين، و`needs: verify` بينهما. لا نشر إلا بعد
اجتياز الفحوص كلها. و`deploy.yml` حُذف.

**ومكسب ثانٍ:** مهمّة النشر لم تعد تبني شيئاً. `site/` مدفوع في المستودع
وحارس الانحراف يثبت أنه يطابق المصدر، والسكربت لا يستعمل إلا وحدات `Node`
المدمجة — فلا `npm ci` ولا بناء. النشر صار خطوة واحدة.

**وتغيير في عادتي:** أتوقّف عن تشغيل `publish-deploy-branch.mjs` من عندي بعد
كل دفعة. سير العمل هو مسار النشر، وتشغيلي اليدوي كان يصنع الكوميت الثاني —
ويلتفّ حول البوابة كما في `I-015`. يبقى السكربت للطوارئ، وبوابته الذاتية
معه.

**الحالة:** مغلقة.

---

## I-018 · المقال الشتوي — موضوعه ابتلعته صفحة الحل

**الخطورة:** 🟡 محتوى

**العَرَض:** `darfor-laddar-mobilen-samre-pa-vintern` عنوانه «لماذا يشحن الهاتف
أسوأ في الشتاء»، **وجوابه صار منشوراً على `mobilen-dor-i-kylan`** بعد أن
نُقلت إليها مصادر `Apple`. صفحتان لنا على استعلام واحد.

**السبب مسجَّل باعتراف صاحبه:** `Claude Project` نقل المصادر إلى صفحة الحل،
وكان صواباً في حينه، **وأفرغ المقال**.

**قرار `Claude Project`:** يُعاد تحديد الموضوع لا يُحذف — الشتاء وبقيّة العدّة
لا الهاتف: البطارية المحمولة في البرد · الكابلات · اللاصق المغناطيسي ·
السيارة.

**واقترح إبقاء الـ`slug` وقال إن تغييره «يكلّف إعادة توجيه بلا عائد».**

**قياسي يُسقط هذه الفرضية:**

```
stage: draft · published: true · updated: 2026-09-03
robots: noindex, follow
في sitemap: 0
```

الصفحة **هيكل معلَن نُشر اليوم**، `noindex` وخارج الخريطة. **فلا ترتيب يُفقد
ولا رابط خارجي يُكسر** — تغيير الـ`slug` تكلفته صفر، لا «بلا عائد».

**فالثمن معكوس:** إبقاء `slug` لا يصف الصفحة كذبةٌ صغيرة دائمة — وهو المعيار
الذي رفض به `Claude Project` نفسه إبقاء `flyget` في عنوان صفحة السفر.

**الموصى به:** يُغيَّر الـ`slug` مع الموضوع. وإن أراد احتياطاً فـ`redirects`
في `astro.config.mjs` موجودة أصلاً وسطرٌ واحد يكفي.

**القرار `2026-09-03`:** `Claude Project` قبل القياس وغيّر المسار إلى
`powerbank-i-kyla`، وأضيف سطر `redirects` رغم أن لا أحد يحتاجه اليوم.

**والتغيير كشف موضعين لم ينتبه لهما أحد:** قائمة الموجة الأولى في
`make-state.mjs` كانت تحمل المسار القديم — **وصرخ الحارس باسمه** بدل أن يخفض
العدّ صامتاً، وهو ما بُني له بعد `EB-018`. وفحص الصفحات كان يعامل صفحة إعادة
التوجيه المولّدة كمحتوى، فيطلب منها `h1` ووصفاً. استُثنيت صفحات `meta refresh`.

**الحالة:** مغلقة.

---

## I-019 · مصدر المواصفات في كل منتج هو صفحة بائع، لا صفحة مصنّع

**الخطورة:** 🟠 توثيق · يمسّ أربع صفحات حيّة

**كيف ظهر:** `Claude Project` خطّط للموجة الثانية على أنّ سبعة منتجات لها
`asin` **وينقصها `source_url` و`last_verified`**. قِستُ الملفات العشرين قبل
الموافقة، فالتشخيص غير دقيق — والواقع أثقل منه:

| | العدد |
|---|---|
| `verified: true` | ٤ |
| لها `asin` و`source_url` و`last_verified`، و`verified: false` | ٧ |
| بلا `asin` | ٩ |

**السبعة لا ينقصها مصدر ولا تاريخ — الثلاثة حاضرة في كل ملف منها.** الفارغ
فيها `pros` و`cons` و`best_for`: حقول حكم تحريري، لا إدخال بيانات. أي أنّ
الموجة الثانية ليست محجوزة على بيانات ناقصة، بل على كتابة لم تُكتب — وهي عمل
`Claude Project` نفسه.

**والأثقل:** `source_url` في **العشرين جميعاً** رابط `amazon.se` — بما فيها
الأربعة الموثقة والمعروضة على الموقع الآن. و`key_specs` مأخوذة من عنوان
الإعلان، والتعليق في كل ملف يقولها صراحة. **صفحة بائع تكفي للربط ولا تكفي
لتوثيق مواصفة**، وعنوان إعلان يكتبه البائع لا المصنّع.

**ولا يُفتح الباب بقلب العلَم:** الشرط المكتوب في الملفات هو
«`verified: true` بعد اكتمال `asin` و`source_url` و`last_verified`» — وهو
**مكتمل في السبعة**. لو أخذنا الشرط حرفياً لصارت السبعة موثقة اليوم بمواصفات
من عناوين إعلانات. الشرط ناقص، لا العلَم متأخّر.

**ما بُني الآن:** القاعدة `2b` في `validate.mjs` — منتج `verified: true`
مصدره نطاق بائع (`amazon` · `ebay` · `cdon` · `komplett` · `netonnet` ·
`elgiganten` · `webhallen`) **يرفع تنبيهاً باسم الملف**. تنبيهاً لا خطأً،
لأن أربع صفحات حيّة عليه وإسقاط البناء عليها يخفي المشكلة بدل حلّها.

**متى يصير خطأً:** حين يحمل كلٌّ منها أحد المصدرين أدناه. عندها تُقلب `2b`
إلى `errors` بسطر واحد، ولا يعود ممكناً توثيق منتج من صفحة بائع.

---

**اعتراض `Claude Project` `2026-09-04`، وقُبل:** اشتراط صفحة المصنّع على
الكابلات وحدها **تشديدٌ لم نطبّقه على المنشور** — فالبطاريات الأربع الحيّة
مصدرها `amazon.se` وهي `verified: true`. والاعتراض صحيح: قاعدة تُطبَّق على
الجديد وتُعفى منها الصفحات القائمة ليست قاعدة.

**واقتراحه المقبول — مصدران لا واحد:**

```
verified: true يحتاج أحدهما:
  (أ) source_url على صفحة المصنّع
  (ب) spec_photo — صورة من تصويرنا تُظهر المطبوع على الجهاز أو علبته
```

**و(ب) ليست تنازلاً — هي أقوى في موضع واحد حاسم:** صفحة المصنّع تصف **طرازاً**
قد لا يكون الذي في اليد؛ وصورتنا تصف **الجهاز الذي نملكه بعينه**. وهذا أصل
الموقع كلّه: الملكية هي الميزة.

**وبُني الحقل بحيث لا يُدّعى:** `spec_photo` يجب أن يساوي `src` إحدى صور
`own_photos` نفسها، وإلا **فهو خطأ يُسقط البناء** — لا تنبيه. صورةٌ مذكورة
بلا ملف هي ادعاء، وهو ما تمنعه القاعدة.

**وحدّه المعروف:** ما ليس مطبوعاً على الجهاز لا تُثبته صورته. طول الكابل
وقدرته مثالٌ حيّ — `P-22` لا يحمل طوله مطبوعاً، فبقي مصدره ناقصاً ومكتوباً
في رأس الملف.

**أول تطبيق `2026-09-04`:** رفع حسام ثلاث صور، فصار `P-21` **أول منتج في
المشروع موثَّقاً من دليلنا نحن**: أسماء المنافذ الثلاثة و`100W` مقروءة كلّها
في `P-21-1`، فحملت `spec_photo` وقُلبت `verified`.

**وفي الدفعة نفسها عضّت القاعدة حدَّها:** `P-22` وصلت صورته أيضاً، وتُثبت
`Hölje` و`Kontakter` — **ولا تُثبت `Effekt` ولا `Längd`**، فليسا مطبوعين على
الكابل. فبقي `verified: false`، والسبب مكتوب في رأس ملفه. قاعدةٌ تُطبَّق على
منتجين في يوم واحد وتقول «نعم» لأحدهما و«لا» للآخر هي قاعدة تعمل.

**النظر تمّ في `2026-09-04`:** فُتحت صور البطاريات الأربع. `P-03` وحدها كان
عليها رقم مطبوع — وكان يناقض الملف، فأنتج `I-023`. والثلاث الأخريات لا يحمل
أيٌّ منها مواصفةً مطبوعة مقروءة: `Anker MagGo` شاشةٌ وحامل، و`INIU` شعارٌ
و`Qi2`، و`Zapix` اسم العلامة. لا تقصير في التصوير — **المصنّع لم يطبع شيئاً.**

---

**والشرط سقط بطلب حسام في `2026-09-04`، وهو محقّ:** كان يقول إنّ صورته لا
تُوثّق إلا إن حملت مواصفةً مطبوعة. **فكان يعاقب المالك على تصميم المصنّع.**

**فصار المصدر ثلاثة لا اثنين:**

```
(أ) source_url على صفحة المصنّع
(ب) spec_photo — صورتنا تُظهر المطبوع على الجهاز
(ج) owner_checked — حسام فتح صفحة البائع وقارنها بالجهاز الذي بيده، بتاريخه
```

**و(ج) ليس أضعفها.** هو الذي كشف أنّ `ASIN` في `P-03` كان لطراز آخر — عطبٌ
لم تكن أي صورة لتكشفه، ولا صفحة مصنّع. صفحة المصنّع تصف طرازاً، والصورة تصف
جهازاً، **وتحقّق المالك وحده يربط الاثنين بالرابط الذي سيضغطه القارئ.**

**والفرق بينها مسجَّل ولا يُطمس:** (أ) و(ب) يستطيع غيرُنا التحقق منهما، و(ج)
شهادةُ مالكٍ مؤرَّخة باسمه. ولذلك يُكتب التاريخ في الملف ويُقرأ.

**النتيجة المقيسة:** تنبيهات `2b` **من خمسة إلى صفر**. وكل منتج موثَّق في
المشروع صار له مصدر مسمّى.

**الحالة:** مغلقة.

---

## I-020 · قرار الهياكل يصطدم بخط المراجعة — صفحة حيّة تختفي

**الخطورة:** 🟠 قرار مالك · يمسّ ما يراه الزائر اليوم

**كيف ظهر:** `laddkabeln-gar-sonder` كانت **هيكلاً منشوراً حيّاً** بقرار حسام —
رابط قائم، يقول للزائر إنّ القسم لم يُكتب ويدعوه ليكتب. ووصل نصّها من
`Claude Project` بـ`stage: written` و`published: false`، وهو **موافق للقاعدة
`5b` حرفاً**: النصّ غير المراجَع لا يُنشر.

**والنتيجة المقيسة:** بعد البناء، `site/sv/solutions/laddkabeln-gar-sonder/`
**لم تعد موجودة**. الرابط الحيّ اختفى — لا صفحةَ نصٍّ ولا هيكل.

**فالقراران يتصادمان، وكلاهما لحسام:**

| القرار | ما يقتضيه هنا |
|---|---|
| «الهياكل تبقى ظاهرة حتى يكتمل الموقع» (`I-016`) | الرابط لا يختفي |
| «نصّ سويدي غير مراجَع لا يُنشر» (`5b`) | النصّ لا يظهر |

**والحلّ التقني موجود ولم يُطبَّق لأنه تغيير قاعدة لا تنفيذ مهمة:** القالب
يعرض الهيكل حين `stage === 'draft'` وحدها. لو صارت `draft` أو `written`،
لصار معنى الحقلين مستقلاً ونظيفاً:

```
published   هل الرابط موجود على الموقع؟
stage       إلى أين وصل النصّ؟
```

فتصير `written` + `published: true` = **الرابط حيّ، والزائر يرى الهيكل،
والنصّ ينتظر المراجعة في الملف**. لا نصّ غير مراجَع يظهر، ولا رابط يختفي.
والتحقّق: لا صفحة واحدة اليوم تحمل `written`/`reviewed` مع `published: true`،
فالتغيير لا يمسّ شيئاً قائماً. ويلزم معه توسيع مرشّح `sitemap` ليشمل
`written`، وشارة `Inte skriven än` في البطاقات.

**لماذا لم أفعله فوراً:** `5b` قاعدة مكتوبة بعد قرار الهياكل وبوعي بالحالات
الثلاث. تغييرها قرار حاكم لا تنفيذ مهمة. فنُفِّذ الوارد كما ورد، ورُفع القرار.

**قرار حسام `2026-09-04`: «أبقِ الهيكل».** وأيّده `Claude Project` بحجّة
تحريرية تستحق التسجيل: الهيكل يقول «لم تُنشر بعد» عن صفحة نصّها مكتوب وينتظر
المراجعة — **وهذا صادق من موضع القارئ**، إذ لم يُنشر له شيء فعلاً. فلا حاجة
إلى حالة رابعة ولا إلى نصّ جديد.

**ما نُفِّذ:**

| الموضع | التغيير |
|---|---|
| القوالب الأربعة | `notWritten = stage === 'draft' \|\| stage === 'written'` |
| بطاقات الرئيسية | الشارة تتبع القالب حرفاً، فلا تختلف البطاقة عن الصفحة |
| `sitemap` | المرشّح يستثني `draft` و`written` معاً |
| القاعدة `5b` | لم تعد تمنع `written` + `published: true`. وبقي المنع على `reviewed`: نصّ تامّ مراجَع منشورٌ بلا قرار نشر |

**والمعنى صار سطرين لا يلتبسان:**

```
published   هل الرابط موجود على الموقع؟
stage       إلى أين وصل النصّ؟
```

**الحالة:** مغلقة.

---

## I-021 · أربع صفحات ثابتة منشورة ونصّها يقول عن نفسه إنه مسودّة

**الخطورة:** 🔴 حيّ على الموقع الآن · يمسّ الحجّة القانونية

**ظهرت في مراجعة `2026-09-04` الشاملة.** الحارس `check:drafts` كان يبلّغ عنها
منذ بنائه، **ولم ينظر أحد إلى تبليغه** — لأنه لا يُسقط البناء.

| الصفحة | ما يراه الزائر السويدي اليوم | كلمات فعلية |
|---|---|---|
| `om-oss` | سطر واحد: «⚠️ UTKAST. Skriv den riktiga texten innan lansering.» **وهذا كل الصفحة** | **٠** |
| `integritetspolicy` | «⚠️ UTKAST — **SKA INTE PUBLICERAS SOM DEN ÄR**» ثم «Fylls i.» تحت كل عنوان | ٢٨ |
| `cookies` | «⚠️ UTKAST. المحتوى يتوقف على أداة التحليلات» | ١٥ |
| `annonssamarbete` | نصّ حقيقي، وفوقه تحذير بمراجعته أمام `Marknadsföringslagen` | ١٤١ |

**والثلاث الأولى في التذييل، على كل صفحة من الموقع.**

**ولماذا هذا أسوأ من صفحة غائبة:** سياسة خصوصية تقول عن نفسها «يجب ألّا تُنشر
كما هي» **ليست سياسة**. والإفصاح التجاري وسياسة الخصوصية هما حجّتنا أمام
القانون السويدي، وحجّةٌ موسومة بأنها غير مراجَعة تُضعف نفسها. أمّا `om-oss`
فصفحةٌ فارغة تماماً يصل إليها الزائر من التذييل.

**وما صار ممكناً اليوم ولم يكن:** بعد `I-020` صار الفصل بين «الرابط موجود»
و«النصّ وصل» مبنيّاً في القوالب الأربعة. **ولم يُمدّ بعد إلى مجموعة `pages`** —
مخطّطها لا يحمل `stage` أصلاً. ومدُّه إليها يجعل الزائر يرى إعلاناً نظيفاً بأن
الصفحة لم تُنشر بعد، مع دعوة للتواصل، بدل مسودّة تتنكّر في صورة سياسة.

**والفرق الذي أراه:** `annonssamarbete` نصّها حقيقي وتحذيرها تحريري — تُعالَج
بحذف التحذير بعد مراجعة. والثلاث الأخرى **لا نصّ فيها أصلاً**.

**قرار حسام `2026-09-04`:** «قم بتعبئتها من عندك فأنا أوكّلك بذلك» — والنصوص
الأربعة كُتبت في الدفعة نفسها.

**وما كُتب، بالمبدأ الذي حكمه:** لا جملة إلا وهي صحيحة عن الموقع كما هو اليوم.

| الصفحة | ما تقوله الآن |
|---|---|
| `cookies` | **لا كوكيز إطلاقاً** — مقيسة: لا `localStorage` ولا `sessionStorage` ولا `document.cookie` في مخرَج البناء كلّه، ولا نطاق خارجي يُحمَّل منه شيء. فلا نافذة موافقة |
| `integritetspolicy` | المسؤول مسمّى · لا شيء يُجمع عند القراءة · الأساس القانوني «مصلحة مشروعة» للردّ على رسالة · الحقوق الستّة · و`IMY` جهةَ الشكوى |
| `om-oss` | من يكتب، ولماذا، وحدّان معلَنان: لا نشتري عشرين منتجاً للمقارنة، والسويدية ليست لغةً أولى |
| `annonssamarbete` | ما كان صحيحاً بقي · وأُضيف: أمازون بالاسم · ما لا يحكم الترشيح · كيف تُعرف الرابطة التجارية · وأن الإرجاع مع البائع |

**والحارس قُلب:** `check:drafts` صار `FATAL = true`. صفحة منشورة بنصّ مسودّة
**تُسقط البناء** اليوم، بدل أن تمرّ عشرة أسابيع بلا أن يقرأها أحد كما مرّت.

**وما يبقى معلّقاً وليس منها:** جملة الإفصاح الحرفية من حساب أمازون — نصّهم لا
صياغتنا. مكانها جاهز في `annonssamarbete`، وهي `T4` في الخطة.

**الحالة:** مغلقة.

---

## I-022 · التسليم الأول اختُبر بجلسة جديدة فسقط في أربعة مواضع

**الخطورة:** 🟠 عملية · لا تمسّ الموقع

**كيف ظهر:** سُلّم المشروع في `2026-09-04`، وفتح حسام جلسة جديدة على الوثائق
نفسها. **فأنتجت أربعة أسئلة قبل أن تعمل شيئاً** — وثلاثة منها ثغرات في التسليم
لا في المشروع.

| ما سألت عنه | الخطأ | لماذا |
|---|---|---|
| «`TASKS` تقول موثقة ٥ و`STATE` يقول ٧» | **تسليمي** | كتبتُ أرقاماً بيدي في `TASKS`، ثم وثّقتُ منتجين بعدها بساعتين ولم أعد إليها. **وكتبتُ في `HANDOVER` أنّ الأرقام مولَّدة لا يدوية — في اليوم نفسه** |
| «سبعون ملفاً تغيّرت بعد `verify`» | **تسليمي** | أعرف أنه ختم البناء وأتعامل معه منذ الصباح، **ولم أكتبه في التسليم سطراً واحداً** |
| «بيئتي تفرض فرعاً وأنت قلت `main`» | **تسليمي** | كتبتُ «ادفع إلى `main`» من واقع جلستي، وفرضتُه على بيئة لا أعرفها |
| «سبعة تنبيهات» | لا أحد | مسجّلة في `I-019`، لكنها بدت مفاجئة لأن التسليم لم يقل إنها معروفة |

**والخلاصة التي تستحق التسجيل:** أسئلة الجلسة الجديدة **ليست ضجيجاً — هي نتيجة
اختبار.** كل ما لم يُكتب في التسليم لا وجود له عند من يتسلّم، مهما كان بديهياً
عند من سلّم.

**وخطأ في الأسلوب لا في المضمون:** رفعت تلك الجلسة إلى حسام اختياراً بين
أمرَي `git`. وهو ليس مبرمجاً، وليس هذا قراره.

**ما نُفِّذ:**

| # | الإصلاح |
|---|---|
| ١ | **حُذف كل رقم يدوي** من `TASKS` — يُحذف لا يُصحَّح، فما دام موجوداً سيتقادم. والأرقام تُقرأ من `npm run state` وحده. و`HANDOVER §2` صار يحيل إلى الأمر بدل أن يسرد أرقاماً |
| ٢ | `HANDOVER` قسم **`4b` — ضجيج معروف**: ختم البناء وتراجُعه المُوجَّه، والتنبيهات السبعة بحالتها، والفرع |
| ٣ | `INSTRUCTIONS v1.8` — بند «لا تُصعِّد الضجيج الميكانيكي»، وحدّان صريحان لما هو قرار حسام وما ليس |
| ٤ | تسلسل البدء صار يتضمّن التراجع المُوجَّه بعد `verify` |

**ودرسٌ دُفع ثمنه أثناء كتابة هذا:** كتبتُ في `4b` أنّ العلاج `git restore .`،
ثم شغّلتُه لأختبره **فمحا سبعة ملفات وثائق كنتُ أكتبها ولم تكن مودَعة**. أُعيدت
كتابتها، وصار العلاج **مُوجَّهاً إلى مواضع الضجيج الثلاثة وحدها**، والتحذير
مكتوب في `4b` بثمنه.

**وقرار حسام على تكرار نصّ التجربة:** يُترك — سيعيد كتابة النصوص بنفسه. سُجّل
في `4b` كي لا يُرفع كاكتشاف جديد في كل جلسة.

**الحالة:** مغلقة.

---

## I-023 · زرّ الشراء كان يبيع طرازاً غير الذي تصفه الصفحة

**الخطورة:** 🔴 كان حيّاً على سبع صفحات · يمسّ القارئ وحساب أمازون

**كيف ظهر — وهو نموذج لما بُنيت له القاعدة `2b`:** جلسة `Claude Code` جديدة
قرأت في `I-019` بنداً يقول إنّ صور البطاريات الأربع **«لم يُنظر بعد»** هل
تغطّي مواصفاتها. فنظرت. ووجدت في صورة `P-03` رقماً مطبوعاً يناقض الملف:

```
الملف:    Effekt: 165 W        ← مأخوذة من عنوان إعلان
الجهاز:   145W                 ← مطبوعة على الجسم في صورتنا
```

**ثم اتّسع العطب عند التحقق:** `UGREEN` تصنع طرازين من هذه البطارية، `145W`
و`165W`. فلو صُحّح الرقم وحده لبقي الرابط على الطراز الآخر. راجع حسام سجلّ
شرائه فتبيّن أنّ `ASIN` في الملف **لمنتج آخر**:

| | كان | صار |
|---|---|---|
| `asin` | `B0DSPX4RQ5` | **`B0BJQ7F16T`** |
| `Effekt` | `165 W` | **`145 W`** |
| مصدر المواصفة | عنوان إعلان | **`spec_photo` — صورتنا** |

**ولماذا هذا أخطر من رقم خاطئ:** الصفحة تصف جهازاً، والزرّ يبيع جهازاً آخر.
القارئ يشتري غير ما قرأ — وهو ما يهدم الثقة، ويعرّض حساب الشراكة.

**ما صُحِّح:** الاسم · `key_specs` · أول `pros` · جملة الترشيح في
`basta-powerbank-2026` · `asin` · `source_url` · واسم ملف الصورة حمل الـ`ASIN`
القديم فتغيّر إلى `P-03-1-B0BJQ7F16T.webp` (وهذا يقطع خزن المتصفّح أيضاً —
`C-26`).

**والمكسب:** `P-03` صار **موثَّقاً من صورتنا**، وسقط تنبيه من الخمسة.

**والدرس البنيوي:** القاعدة `2b` لم تكن تصرخ في وجه رقم خاطئ — كانت تقول
«المصدر صفحة بائع» فقط. **والذي كشف العطب هو أنّ أحداً نظر إلى الصورة.**
فالبند المفتوح في `I-019` — «لم يُنظر بعد» — كان يستحق أن يُكتب، ولولا كتابته
لما نظر أحد.

**وما يبقى:** ثلاث بطاريات (`P-01` · `P-02` · `P-04`) صورها لا تُظهر مطبوعاً
مقروءاً. تحتاج صورة قريبة لظهر كل جهاز.

**الحالة:** مغلقة على `P-03` · والباقي في `I-019`.

---

## I-024 · حذف عشر وثائق ترك اثنتي عشرة إشارة تقود إلى لا شيء

**الخطورة:** 🟡 ثقة

**العَرَض:** في تنظيف `2026-09-05` حُذفت عشر وثائق. وبقيت الفهارس والسجلات
تشير إليها: `docs/README.md` يعرض أربعة أسطر لملفات لم تعد موجودة،
و`REFERENCE_SYSTEM.md` يحيل القارئ إلى `DEPLOY_FIX.md`، و`ISSUES.md` نفسه يقول
«الخطوة التالية في `REMOVE_WORDPRESS.md`».

**لماذا يؤذي أكثر من غياب الوثيقة:** القارئ يرى رابطاً فيظن أن الجواب موجود،
فيبحث عنه ولا يجده. والغياب الصريح يوجّهه إلى السؤال؛ والرابط الميت يوجّهه إلى
الدوران. **وهذا يتضاعف منذ صار المستودع الذاكرة الوحيدة** — `D-029`.

**كيف وُجدت:** باليد، بعد الحذف. **ولم يكن في البوابة ما يراها** — أربعة عشر
فحصاً ولا واحد يفتح رابطاً في وثيقة.

**الحارس الذي بُني بعده:** `scripts/check-links.mjs` ضمن `npm run verify`.
يفتح كل رابط `Markdown` نسبي في كل وثيقة ويسقط إن لم يجد الملف. جُرِّب بإضافة
رابط ميت متعمَّد فأسقط البناء، ثم حُذف فمرّ.

**الحالة:** مغلقة — الإشارات الاثنتا عشرة أُصلحت، والحارس يمنع تكرارها.

---

## مغلقة

| # | المشكلة | كيف حُلّت |
|---|---|---|
| C-01 | مسارات ويندوز تكسر السكربتات | `fileURLToPath` بدل `.pathname` |
| C-02 | التصفية لا تعمل — الأزرار بلا أثر | السكربت كان يعمل قبل رسم العناصر |
| C-03 | التصفية تتلوّن ولا تصفّي | `display:flex` يتغلب على `hidden` |
| C-04 | زر "الكل" لا يعيد العرض | كان يمسح الفئة ويترك مرشّح التجربة |
| C-05 | النص ينكسر بلا داعٍ | قيد `52ch` ثابت على النص التمهيدي |
| C-06 | الهوية على الرئيسية فقط | شريط `--brand-soft` أُضيف لكل صفحة |
| C-07 | فئات فرعية فارغة في القائمة | تُخفى تلقائياً بلا محتوى منشور |
| C-08 | بقايا خضراء في الحزمة | ملفا `SVG` قديمان وشاشة اللوحة |
| C-09 | رمز المنتج يتغيّر عند تعديل البيانات | حقل `code` ثابت في كل ملف بدل ترتيب محسوب |
| C-10 | حقل `code` وُضع في مخطط الفئات بالخطأ | استبدال نصي أصاب أول تطابق — **درس 6.4** |
| C-11 | كتلة CSS كاملة مكررة (3634 حرفاً) | حُذفت، وأُضيف `npm run check:css` |
| C-12 | استبدال نصي أفسد قواعد الصور | أُعيدت كتابة الكتلة، والفاحص يمنع التكرار |
| C-13 | كلمة `prylar` تحمل ارتباط الطرائف | أُعيدت صياغة كل النصوص بلا اسم فئة |
| C-14 | العارض يُقصّ داخل البطاقة | `transform` يجعل البطاقة حاوية للعنصر الثابت |
| C-15 | تأثير التمرير «عالق» على اللمس | لُفّ في `@media (hover: hover)` |
| C-16 | تعارض لوحة الشعار مع الموقع | اللوحة وُحِّدت على ألوان الشعار |
| C-17 | «نستخدم الأربعة» في نصّ وعنوان دليل منشور بينما اثنان `tested: false` | صيغة الملكية، والقاعدة `5e` تقارن الكمّ بالبيانات |
| C-18 | تسمية شارة `best-overall` تبدأ بـ«Bäst i test» الممنوعة نصّاً | صارت «Vårt förstahandsval»، والقاعدة `5d` تمنع العبارة حتى في ملفات الترجمة |
| C-19 | الرئيسية تقول «لا نختبر المنتجات فيزيائياً» بينما الملكية هي الميزة | صيغت الأربع على «نملك ونستعمل، ولا نقيس بأجهزة» |
| C-20 | سلسلة «مملوك غير مُختبَر» تقول «نملكها **ونستعملها**» | أُعيدت صياغتها، والقاعدة `5f` تربط السلسلة بشرط عرضها |
| C-21 | «använd isedan juni 2025 · sedan2025-06» — كلمتان ملتصقتان والتاريخ مرتين | تُبنى الأسطر في الواجهة الأمامية لا في القالب |
| C-22 | `var(--ink-3)` على رمز غير معرَّف يرث اللون بصمت | `check:css` صار يتحقق من كل رمز، ويقرأ الرموز المضبوطة من القوالب |
| C-23 | صفحة المنهج المنشورة تشرح هويّة قديمة نفتها الواجهة | أُعيدت كتابتها على الحالات الثلاث |
| C-24 | قاعدة `5d` كانت ستمنع الوعد بترك العبارة الممنوعة | صارت تُعفي النفي كما في قاعدة الادعاء |
| C-27 | صورة واحدة بالبايت نفسه معروضة على `P-01` و`P-02` بوصفين مختلفين | لقطة مقارنة لا يُعرف فيها أيّ جهاز هو المقصود. استُبدلت بصور مفردة لكل منتج، وحُذفت |
| C-26 | صورة `P-20` صحيحة في المستودع وخاطئة عند الزائر | العنوان `P-20-1-B0GV79Z1W6.webp` كان يخدم صورة الكابل ذي الشاشة قبل التصحيح، **والمتصفّح يخزّن بالعنوان لا بالمحتوى**. القاعدة: **محتوى جديد ← اسم جديد**. صار `…-v2.webp` |
| C-25 | صورة كابل **بشاشة** كانت معروضة على `P-20` وهو الكابل **بلا شاشة** — والوصف يقول «hoprullad bredvid en telefon» | حسام لمحها. فُصلت إلى `P-23`، وصارت **مصدر مواصفته** بالقاعدة `2b`: «240W» والشاشة والطرفان مقروءة فيها كلها |
| I-008 | عَرَض الطيران محذوف من صفحة السفر | `powerbank-pa-flyget` نُشرت، فعاد العَرَض الثالث. **والعنوان لم يعد** — القرار دائم |
