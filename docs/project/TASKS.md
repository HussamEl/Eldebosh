# المهام الحالية

> **لا أرقام هنا.** الأعداد وما ينقص كل منتج في `STATE.md`، مولَّدة آلياً:
>
> ```
> npm run state
> ```
>
> المهمة المنجزة تُحذف من هنا وتُذكر في `CHANGELOG.md`. هذا الملف يصف ما بقي فقط.

---

## الآن — بالترتيب

| # | المهمة | على من | تُعدّ منجزة حين |
|---|---|---|---|
| **T1** | **مراجعة صفحتي الحلّ المكتوبتين:** `mobilen-laddar-langsamt` ثم `laddkabeln-gar-sonder`. لا تُنشر الثانية قبل الأولى لأن نصّها يحيل إليها | حسام | المرحلة `published` للاثنتين |
| **T2** | **أرقام ASIN الناقصة.** بلا رقم لا زرّ شراء ولا دخول في أي دليل. القائمة في `STATE.md` تحت «ما ينقص كل منتج» | حسام | لا منتج مملوك بلا ASIN |
| **T3** | **مزايا وعيوب ولمن يصلح** لكل منتج، ثم نصوص الموجة الثانية (الشحن والكابلات) | كلاودي كود يكتب · جيميناي يراجع | الحقول ممتلئة في `STATE.md` |
| **T4** | **مهلة الهياكل:** ١٨ صفحة هيكل مهلتها **`2026-12-02`**. بعد هذا التاريخ **يسقط البناء** ولا يُنشر شيء حتى تُكتب أو تُسحب. القائمة في `STATE.md` | كلاودي كود يكتب · جيميناي يراجع · حسام يقرّر ما يُسحب | لا هيكل تجاوز مهلته |
| **T5** | **نصّ التجربة مكرّر حرفياً** في ثلاثة منتجات (`P-01` · `P-04` · `P-11`). المطلوب جملة حقيقية واحدة عن كل منتج لاحظتها بنفسك: زرّ يصعب ضغطه بالقفاز، سخونة في الجيب… تكتبها بالعربية ويصوغها كلاودي كود بالسويدية | حسام | الفحص بلا تنبيه تكرار |
| **T6** | **الوزن غائب** عن مواصفات البطاريات الأربع (`P-01` حتى `P-04`)، وقسمان في صفحتين يتحدّثان عن الوزن بلا رقم. يُؤخذ من صفحة المصنّع | كلاودي كود · جيميناي يتحقق | الوزن في البيانات بمصدره |

## قرارات تنتظر حسام

| # | المسألة | توصيتي |
|---|---|---|
| **Q1** | الشعار الكامل يحمل عبارة **BÄST OCH SMART**. صيغة تفضيل مطلق، والموقع كله مبنيّ على رفض «الأفضل» بلا دليل. لا تظهر في رأس الموقع، لكنها في ملفات الشعار والمطبوعات | تُستبدل بعبارة وصفية بلا تفضيل |
| **Q2** | صندوق الإفصاح يكرّر كلمة **Annonssamarbete** مرتين: عنواناً ثم في أول الجملة | تُحذف من أول الجملة |
| **Q3** | **الضريبة:** هل يُعامل دخل الأفلييت كنشاط الساحة أم منفصلاً؟ لا يُبنى على تخمين | استشارة مصلحة الضرائب، وهي مجانية |
| **Q4** | **صيغة إفصاح أمازون السويدية.** القائمة تعمل ومنشورة؛ المتبقّي التأكد من الصيغة الرسمية في حساب أمازون، قسم سياسات البرنامج | تُنسخ حرفياً إن وُجدت، وإلا يُغلق البند |

## قبل الإطلاق

| # | المهمة | تُعدّ منجزة حين |
|---|---|---|
| **L1** | أدوات القياس: `Search Console` وأداة تحليلات **بلا كوكيز** (اختيارها قرار حسام) | الخريطة مقبولة بلا أخطاء · لا نافذة موافقة |
| **L2** | فحص الأداء `Lighthouse` على الجوال: الرئيسية · حلّ · دليل · مقارنة | الأداء ٩٥ فأكثر · الوصولية ١٠٠ · SEO ١٠٠ |
| **L3** | إخفاء ختم الإصدار بجانب الشعار: سطر واحد في `src/lib/build-stamp.ts` | لا ختم ظاهر، و`verify` أخضر |

## لاحقاً — لا يُنفَّذ الآن

- **واجهة أمازون البرمجية** بعد ثلاث مبيعات مؤهَّلة: وحدها تفتح الصور والأسعار.
- فئات جديدة · شبكات أفلييت غير أمازون · محتوى إنجليزي · فيديو لكل عنقود.
