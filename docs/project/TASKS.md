# TASKS.md — خطة العمل v2.1

> **أُعيدت كتابتها في `2026-09-04`.** كانت `v1.0` تصف مشروعاً في أوّله: النشر
> عبر `FTP`، ولوحة التحرير «المهمة القائمة»، وعنقود البطارية لم يُكتب. كلّ ذلك
> تجاوزه الواقع، وخطّةٌ لا تصف الواقع تُضلّل من يقرؤها.
>
> **`v2.1` في `2026-09-04`: لا رقم في هذا الملف.** كانت `v2.0` تحمل كتلة أرقام
> مكتوبة بيدي، فتقادمت في ساعتين وصارت تقول «موثقة ٥» بينما المولَّد يقول ٧ —
> فوجدها من تسلّم المشروع وسأل: أيّهما أصدق؟
>
> **الجواب البنيوي: يُحذف الرقم اليدوي، لا يُصحَّح.** ما دام موجوداً فسيتقادم
> ثانية.

---

## أين نحن — لا يُكتب هنا، يُقرأ من مصدره

```
npm run state
```

يطبع الحالة كاملة ويحدّث `docs/project/STATE.md`. **هو المصدر الوحيد للأرقام**
— المنتجات والموثقة وما ينتظر رقم أمازون، والوثائق ومراحلها، والموجة الأولى.

| السؤال | الأمر الذي يجيبه |
|---|---|
| كم منتجاً؟ كم موثقاً؟ أين وصل المحتوى؟ | `npm run state` |
| ما الذي يخالف القواعد الآن؟ | `npm run check` |
| هل كل شيء سليم قبل الدفع؟ | `npm run verify` |

**والثابت الذي لا تغيّره الأرقام:** الموجة الأولى — عنقود البطارية — **مكتملة**،
والفئتان مفعّلتان، والنصوص القانونية الستّ مكتوبة.

---

## ما أُنجز — لا يُعاد

| | المهمة | الدليل |
|---|---|---|
| ✅ | البنية والمستودع والدومين و`SSL` | `eldebosh.com` يعمل |
| ✅ | **النشر التلقائي** — لا `FTP`: غيتهب يبني، والاستضافة تسحب | قيس في `2026-09-04`: ختم البناء الحيّ = وقت البناء |
| ✅ | حذف كل محتوى `DEMO` | `npm run check` بلا تنبيه `DEMO` |
| ✅ | الهوية والخطوط — `woff2` محلية بلا نطاق خارجي | `@font-face` في `global.css` |
| ✅ | عنقود البطارية كاملاً | ١١ صفحة منشورة |
| ✅ | النصوص القانونية الستّ | منشورة بلا `UTKAST` |
| ✅ | الفئتان مفعّلتان | `npm run state` |
| ✅ | **الهياكل ظاهرة** — رابط حيّ يقول إنه لم يُنشر | قرار حسام · `I-020` |

---

## الاختناق اليوم — تحريريّ، لا بيانات

> **`I-019` أُغلقت في `2026-09-05`.** كل منتج موثَّق صار له مصدر مسمّى، بعد
> أن صارت مصادر المواصفة ثلاثة: صفحة مصنّع · صورتنا للمطبوع · تحقّق المالك
> المؤرَّخ. وتنبيهات `2b` من خمسة إلى صفر.
>
> **فما يحجب الأدلة اليوم ليس البيانات بل النصّ:** `pros` و`cons` و`best_for`
> لكل منتج، ثم نصّ الدليل وترشيحاته. وهو عمل `Claude Project` وحده.

**والمصدر مقبول من طريقين (`I-019`):**

```
(أ) source_url على صفحة المصنّع        ← عمل Claude Project
(ب) spec_photo — صورتنا للمطبوع        ← عمل حسام، صورة واحدة
```

| الفئة | لها ASIN | موثقة | ينقصها |
|---|---|---|---|
| `powerbanks` | ٤ | ٤ | مصدر مواصفة — الأربعة على `amazon.se` |
| `kablar` | ٤ | ٠ | مصدر لكلّ |
| `snabbladdare` | ٢ | ٠ | مصدر لكلّ |
| `billaddning` | ١ | **١** | — ✅ أوّل توثيق من صورتنا |
| `skrivbordsstall` | ٢ | ٠ | مصدر لكلّ |

**والفحص يسمّي الناقص باسم ملفه:** `npm run check` · القاعدة `2b`.

---

## التسلسل — لا تُقفز مهمة

### ~~`T0` الصفحات الثابتة الأربع~~ ✅ `2026-09-04`
كُتبت الأربع بتوكيل حسام، و`check:drafts` صار `FATAL = true` — صفحة منشورة
بنصّ مسودّة تُسقط البناء الآن. التفصيل في `I-021`.

### `T1` دليل الكابلات — **الحاجز سقط في `2026-09-04`**
`kablar` فيها الآن **كابلان موثّقان**: `P-20` (بلا شاشة، ٤٠ جيجابت) و`P-23`
(بشاشة، موثّق من صورتنا). والشرط «ترشيحان على الأقل» **مستوفى**.

**فما بقي تحريريّ بحت:** `pros` و`cons` و`best_for` للكابلين، ثم نصّ الدليل
وترشيحاته وأسبابها.

**قبول:** `basta-laddkabel-usb-c` منشور بترشيحين، و`npm run check` أخضر.

**والكابلان الآخران** (`P-13` بشاشة ١٠٠ واط · `P-22` مضفور ١٠٠ واط) يبقيان
خارج الدليل حتى يأتيهما مصدر — ولا يمنع ذلك نشره.

### `T2` مراجعة الصفحتين المكتوبتين
`laddkabeln-gar-sonder` و`mobilen-laddar-langsamt` — نصّهما جاهز في الملف،
ورابطهما حيّ كهيكل.

**قبول:** `stage: published` بعد قراءة حسام. **ولا تُنشر الأولى قبل الثانية** —
نصّها يحيل إليها.

### `T3` الأرقام الثمانية الناقصة
ثمانية منتجات بلا `asin`: بلا رقم لا زرّ شراء، ولا وجود لها في أي دليل.

**قبول:** `npm run state` يقول «تنتظر ASIN ٠».

### `T4` تأكيد صيغة إفصاح أمازون — **الجملة موجودة وتعمل**

**ما هو قائم بالفعل ومنشور:**

```
سويدي:    Som Amazon-partner tjänar vi pengar på kvalificerade köp.
إنجليزي:  As an Amazon Associate we earn from qualifying purchases.
```

وتظهر في **تذييل كل صفحة**، **وأعلى كل صفحة فيها رابط شراء**، وفي
`annonssamarbete`. المصدر: `src/i18n/ui.ts` · المفتاح `amazon.disclosure`.

**فالمتبقّي تأكيدٌ لا إنشاء:** الصيغة الإنجليزية قياسية في البرنامج، والسويدية
ترجمتها بلفظ «Amazon-partner». إن كانت أمازون تنشر صيغة سويدية حرفية مختلفة
تُنسخ حرفياً وتُستبدل.

**أين تُقرأ:** `Amazon Associates Central` ← `Program Policies` /
`Operating Agreement` — قسم الإفصاح.

**قبول:** إمّا نُسخت الصيغة الرسمية، وإمّا ثُبِّت أنّ القائمة تكفي وأُغلق البند.

### `T5` أدوات القياس — بعد أن يصير ما يُقاس
`Search Console` وأداةُ تحليلات **بلا كوكيز** (اختيارها قرار حسام).

**قبول:** الخريطة مقبولة بلا أخطاء زحف · حدث النقر يعمل عبر `data-affiliate` ·
لا نافذة موافقة.

### `T6` فحص الأداء قبل الإطلاق
`Lighthouse` على الجوال: الرئيسية · حلّ · دليل · مقارنة.

**قبول:** الأداء ≥ ٩٥ · الوصولية ١٠٠ · SEO ١٠٠.

### `T7` ما يُحذف قبل الإطلاق
**ختم البناء بجانب زر الهوم** — أداة قياس مؤقتة بطلب حسام.
الإزالة موصوفة في `src/lib/build-stamp.ts`، ومعها قناع الختم في حارس الانحراف.

**قبول:** لا `build-stamp` في `site/`، و`npm run verify` أخضر بعدها.

---

## لاحقاً — لا يُنفَّذ الآن

فئات جديدة · فلاتر · صفحات منتج مستقلة · شبكات أفلييت غير أمازون.

**وواجهة أمازون البرمجية** بعد ثلاث مبيعات مؤهَّلة — وهي وحدها ما يفتح الصور
والأسعار (`INSTRUCTIONS` ٦٫٣). عتبةُ مبيعات لا مراجعةَ موقع.

---

## معيار واحد يسري على كل ما سبق

**`npm run verify` أخضر بعد كل مهمة.** البوابة المحلية كاملة — من اختبارات
الادّعاء إلى حارس لوحة التحرير — ويُضاف إليها على غيتهب: حارس الانحراف.
**مهمةٌ تتركه أحمر لم تكتمل.**
