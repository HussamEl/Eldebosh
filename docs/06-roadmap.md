# ما تبقّى

مرتّب حسب ما يفتح أكبر قدر من العمل. كل بند قابل للتحويل إلى Issue كما هو.
التقييم المعماري لكل بند مصدره ردّ الجهة التي بنت المصدر
بعد التحقق منه هنا.

## 1. ~~النشر~~ — اكتمل

غيتهب يبني وينشر على فرع `deploy`، وHostinger تسحب تلقائياً. الإجراء والتشخيص
في [12-deploy-from-github.md](12-deploy-from-github.md).

**ما يبقى صحيحاً:** لوحة التحرير تكتب في المستودع، فيصل التغيير خلال دقائق عبر
السلسلة — لا فوراً.

## 2. ~~لوحة التحكم بلا تثبيت إصدار~~ — نُفِّذ الحدّ الأدنى

الإصدار **مثبّت** الآن: `@sveltia/cms@0.204.0`، والسبب مكتوب في
`public/admin/index.html` مع إجراء الترقية.

**ما يبقى مفتوحًا:** الملف ما زال يُحمَّل من `unpkg.com` — وهو الخرق الوحيد
لقاعدة «لا مستضيفات خارجية» التي يلتزم بها باقي الموقع. **والأثر محدود**: اللوحة
`noindex` ولا يفتحها زائر، والموقع نفسه لا يحمّل حرفًا من الخارج.

**الأفضل حين يحين وقته:** إنزال الملف داخل `public/admin/`.

## 3. لا خطوة صور في البناء — *أخفّ مما كان*

الصور المرفوعة من اللوحة تنزل خامًا في `public/uploads/`، ولا تصير بطاقة إلا
بتشغيل:

```
python3 scripts/make_tiles.py <الصورة> P-21-1 [ASIN]
```

**وهذا يعمل بأمر واحد منذ `2026-09-04`** بدل أن يتطلّب مجلدًا مؤقتًا ودفعة
كاملة. فالحجّة لخطوة بناء تلقائية بقيت، وثمنُ غيابها انخفض.

## 4. مفردات مواصفات لكل فئة — *قبل تجاوز ~100 منتج*

`key_specs` خريطة حرّة، وجداول المقارنة تجمع مفاتيح المنتجات فتمتلئ بـ«—» عند
التوسّع. تحديد مفردات مضبوطة لكل فئة الآن أرخص من تعديل كل ملف لاحقًا.
وبالمثل: `verifiedProducts()` يحمّل كل المنتجات ليفلترها في الذاكرة — مقبول عند 20،
مُهدر عند 500.

## 5. المحتوى الإنجليزي — *قرار تحريري لا تقني*

3 ملفات MDX إنجليزية مقابل 32 سويدية. الصفحات الإنجليزية الأخرى فهارس تُبنى من
جدول الترجمة نفسه. إضافة مجموعات `*_en` إلى لوحة التحكم تغيير إعدادات فقط —
تُركت عمدًا حتى لا تُنشر شجرة إنجليزية نصف مترجمة.

## 6. لا `site.webmanifest`

الأيقونات موجودة (`icon-192`، `icon-512`) لكن لا manifest، فلا يمكن تثبيت الموقع
كتطبيق. ملف صغير + وسم `<link rel="manifest">` في `BaseLayout`.

## 7. توسيع الفحص الآلي

`tools/verify-site.mjs` يغطي الرئيسية السويدية أساسًا. ما يستحق الإضافة:
فحوص العارض على `/en/` و`/sv/pa-torget/`، وفحص تباين آلي للنصوص فوق `--volt`،
وفحص أن كل صورة منتج لها `width`/`height` (يفعله `audit.mjs` جزئيًا).

## 8. لغة ثالثة — *رخيصة في الكود، مكلفة تحريريًا*

إضافة الرمز إلى `LANGS`، وعمود إلى `src/i18n/ui.ts`، ومجلدات `content/*/xx/`،
وسطر في `astro.config`. مسارات `[lang]` وباني الروابط لا يحتاجان شيئًا.
الكلفة الحقيقية ترجمة المحتوى وصيانته.

---

## ما اكتمل

- ~~نقل الإصلاحات إلى مصدر Astro~~ — المصدر صار داخل المستودع، والإصلاحات الستة
  فيه، والبناء يمرّ من الفحص كاملًا.
- ~~بيانات الاتصال الحقيقية~~ — أُدخلت في المواضع الثلاثة: `torget.yaml`
  (`active: true` + الهاتف)، وصفحة `kontakt`، وكرت العمل.
