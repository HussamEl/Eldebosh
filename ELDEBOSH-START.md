# ELDEBOSH — ملف البدء

> **وُلِّد آلياً في 2026-09-20 22:23 (كارلستاد).** لا تُحرَّره بيدك.
> يُعاد توليده بـ`npm run start:file`، وكل رقم فيه مقروء من المستودع لا مكتوب.
>
> **هذا الملف يُلصق في أول أي محادثة جديدة، مع أي طرف.**

---

## 0. أوّل ما يجب أن تعرفه

**الكود كله هنا، عامٌّ ومفتوح:**

```
https://github.com/HussamEl/Eldebosh
```

**فلا تطلب حزمة ولا ملفات.** إن كان لك وصول إلى الويب فاقرأ المستودع مباشرة —
هو النسخة الحيّة، وهذا الملف حالةٌ لا كود.

**وإن لم يكن لك وصول إلى الويب** فاعمل مما في هذا الملف، وقل صراحةً حين تحتاج
ملفاً لا تراه. **ولا تخمّن محتوى ملف لم تقرأه.**

---

## 1. ما هو المشروع

`eldebosh.com` — موقع محتوى **سويدي** عن الشحن والكابلات والحوامل.

يأخذ القارئ من **مشكلة يومية** («الجوال يفصل قبل المساء») إلى **حلّ عملي** ثم إلى
**منتج مناسب**. وليس متجراً، ولا موقع أخبار تقنية.

| البند | القيمة |
|---|---|
| الدخل | **Affiliate حصراً** — Amazon Associates على `amazon.se` |
| السوق | السويد · كارلستاد |
| اللغات | `sv` لغة المحتوى · `en` واجهة كاملة بمحتوى غير مترجم |
| الاستضافة | Hostinger مع SSL · النشر تلقائي من غيتهب |

**والميزة التي يقوم عليها المشروع كله:** صاحبه **يملك المنتجات فعلاً ويصوّرها
بنفسه**. لا صورة من متجر، ولا «الأفضل في الاختبار»، ولا رقم بلا مصدر. وهذه ليست
نبرة تسويقية — **هي مفروضة آلياً، والبناء يسقط عند المخالفة.**

---

## 2. أين وصلنا — 2026-09-20 22:23

```
منتجات 23 (موثقة 7 · مصوَّرة 13 · تنتظر ASIN 8) · صفحات 32 (منشورة 32) · الموجة الأولى 11/11 · فئات مفعّلة 2/2 · الساحة مفعّلة
```

**وهذه الأرقام مولَّدة.** مصدرها الوحيد:

```
npm run state
```

**فإن وجدت رقماً في أي وثيقة أخرى فهو خطأ يُحذف، لا مصدر يُصدَّق.**

---

## 3. الأطراف — من يفعل ماذا

| الطرف | يملك | لا يفعل |
|---|---|---|
| **حسام** | القرار · التصوير · المراجعة السويدية · حساب أمازون | **ليس مبرمجاً** |
| **Claude Code** | المستودع · التنفيذ · الفحوص · **الدفع** | لا يقرّر تحريراً |
| **Claude Project** | المواصفات · المحتوى · SEO · التحرير | **لا يدفع إلى غيتهب إطلاقاً** |
| **طرف إضافي** | مكان محجوز — بلا اسم حتى يصل منه ردّ | — |

**وحسام هو القناة الوحيدة بين الأطراف.** لا طرف يتصل بطرف.

**والطرف الإضافي يبقى بلا اسم** حتى يصل منه ردّ فعلاً، فيُضاف اسمه ورقم رسالته
في السجل. لا اسم يُكتب قبل أن يعمل صاحبه.

---

## 4. الترقيم — إلزامي في كل رسالة

**افتح كل رد بسطر واحد:**

```
EB-011 · <اسمك> · 2026-09-20 22:23 · رداً على <الرقم> أو خيط جديد
```

| الحقل | القاعدة |
|---|---|
| `EB-###` | **عدّاد واحد لكل الأطراف** — لا عدّاد لكل طرف |
| الاسم | `Claude Code` · `Claude Project` · `Hussam` |
| الوقت | **توقيت كارلستاد** — يُقرأ من الساعة ولا يُقدَّر |

**رقمك التالي الآن: `EB-011`**

### وكل رد يُختم بلوحة الأطراف

```
| | الطرف | الحالة |
|---|---|---|
| ✍️ | كلاودي كود | كتب هذا الرد · EB-011 |
| 🔴 | كلاودي بروجيكت | مطلوب: <إجراء واحد> |
| ✅ | حسام | لا شيء مطلوب |
```

`✍️` كتب · `⏳` ينتظر · `🔴` عليه عمل الآن · `🟡` يمكن أن ينتظر · `💤` غير معنيّ
· `✅` أنجز. **وكل طرف يظهر في كل مرة** — غيابُ طرف من اللوحة هو ما أضاع الخيط
سابقاً.

---

## 5. القواعد التي لا تُخترق

1. **لا اختلاق.** ممنوع اختراع مواصفة أو سعر أو تقييم أو إحصاء. كل رقم يعود إلى
   مصدر مسمّى. وحيث لا مصدر تُكتب **«يحتاج تحققاً»** ولا يُنشر.
2. **التجربة ثلاث حالات فقط** — مجرَّب · مملوك بلا تجربة · غير مملوك. ولكلٍّ ما
   يُسمح فيه، والفحص الآلي يرفض عبارة تجربة بلا منتج مجرَّب يسندها.
3. **لا سعر ثابت.** نطاق سعري فقط.
4. **صور أمازون وأسعارها ممنوعة** حتى ثلاث مبيعات مؤهَّلة تفتح الواجهة البرمجية.
   **عتبة مبيعات لا مراجعة موقع** — لا شيء يعجّلها.
5. **رابط الشراكة يُبنى من `asin` وحده.** لصق رابط أمازون يدوياً يرفضه الفحص.
6. **ممنوع `Bäst i test`** وكل ادعاء قياس بجهاز، ونسخ نصّ المصدر حرفياً.
7. **الإفصاح التجاري إلزامي وظاهر أعلى كل صفحة فيها رابط شراء** — لا في التذييل
   وحده. متطلب قانوني سويدي.
8. **لا أسرار في أي ملف** — ولا في الوثائق.
9. **لا صفحة تُنشأ لتضخيم عدد.** والهيكل المنشور يقول عن نفسه إنه لم يُكتب بعد.

---

## 6. كيف يُضاف منتج — الطلب الأكثر تكراراً

**الوثيقة الكاملة في المستودع:**

```
docs/project/ADDING_A_PRODUCT.md
```

### والخلاصة التي تحتاجها الآن

**الرمز الحرّ التالي: `P-24`** — يُسنَد مرة واحدة ولا يتغيّر، وأسماء الصور
مبنية عليه.

**الفئات المتاحة — تُكتب حرفياً:**

```
laddning-och-strom
    powerbanks · snabbladdare · kablar · billaddning

hallare-och-ordning
    bilhallare · skrivbordsstall · kabelordning
```

**حرف ناقص واحد يُسقط البناء** — والفحص يقترح عليك الاسم الصحيح.

**والملف يوضع في:**

```
src/data/products/sv/<id>.yaml
```

**والصورة لا تعبر المحادثة.** ترفعها من `/admin/`، أو تعطيها لـ`Claude Code`.
والفحص يرفض صورةً مذكورة بلا ملف على القرص.

**و`verified: false` افتراضياً** — المنتج لا يظهر في أي صفحة حتى يكتمل توثيقه.

---

## 7. الوثائق الحاكمة وإصداراتها

| الوثيقة | الإصدار | التاريخ | ما فيها |
|---|---|---|---|
| `INSTRUCTIONS.md` | `v1.0` | 2026-09-20 | القواعد الملزمة |
| `CONTEXT.md` | `v1.0` | 2026-09-20 | الحالة والقرارات وأسبابها |
| `BRIEFING.md` | `v1.0` | 2026-09-20 | تعريف مختصر بلا سياق سابق |

**وما يُلصق لكل طرف:**

| الطرف | يُعطى |
|---|---|
| **Claude Project** | `INSTRUCTIONS.md` + `CONTEXT.md` + `STATE.md` |
| **Claude Code** | **هذا الملف وحده** — ثم يقرأ المستودع |
| **طرف إضافي** | `BRIEFING.md` + `STATE.md`، ثم السؤال |

---

## 8. نقاط الدخول في المستودع — بالترتيب

| الملف | متى يُقرأ |
|---|---|
| `docs/project/HANDOVER.md` | **أولاً وبالكامل** — نقطة الدخول |
| `docs/project/INSTRUCTIONS.md` | القواعد الملزمة |
| `docs/project/TASKS.md` | ما التالي، بمعايير القبول |
| `docs/project/ADDING_A_PRODUCT.md` | عند إضافة منتج |
| `docs/project/DECISIONS.md` | قبل إعادة فتح نقاش |
| `docs/project/ISSUES.md` | قبل إصلاح شيء |
| `CLAUDE.md` | دستور المستودع — يقرؤه `Claude Code` في كل جلسة |

---

## 9. لـ`Claude Code` وحده — أول أربعة أوامر

```
npm ci
npm run verify
git restore site/ docs/project/STATE.md docs/project/SERVER_FILES.md
npm run check:docs
```

`verify` **يجب أن يكون أخضر قبل أي تعديل.**
والسطر الثالث يمسح ختم البناء وحده — ضجيج متوقَّع لا تغيير.
**ولا تكتب `git restore .` أبداً** — تمحو كل شيء.

**والدفع إلى `main` مباشرة** — إذنٌ قائم (`D-028`). وإن فرضت بيئتُك فرعاً باسم
الجلسة فاعمل فيه ثم ادمجه إلى `main` وادفعه واحذفه **في الجلسة نفسها**.
ولا `Pull Request` إلا بطلب حسام صراحةً.

---

## 10. كيف يُخاطَب حسام

- **ليس مبرمجاً.** اشرح بلغة عملية، ولا تطلب منه أوامر طرفية إلا للضرورة.
- **يقرأ بالعربية من اليمين.** ضع الأوامر والمسارات في **أسطر مستقلة داخل كتل كود**.
- **لا تجامله.** الموافقة السهلة على قرار خاطئ أسوأ من الاعتراض.
- **الضجيج التقني ليس قراره** — `git` والفروع والبناء والتنسيق تحسمها أنت.
- **وقراره هو:** المال · ما يراه الزائر · وعدٌ يُقطع باسم الموقع · حساب أمازون ·
  الهوية البصرية.
- **وإن جئته بشيء فأحضِر معه جوابك المقترح.** لا تعطه `(أ)` و`(ب)` ليختار مكانك.

---

*وُلِّد بـ`npm run start:file` في 2026-09-20 22:23 · لا يُحرَّر بيد · يُعاد توليده عند كل تغيّر.*
